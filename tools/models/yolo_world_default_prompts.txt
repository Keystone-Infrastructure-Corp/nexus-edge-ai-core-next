# Default open-vocab prompt vocabulary baked into the YOLO-World ONNX.
#
# The YOLO-World pipeline takes prompts at *export* time (not inference
# time) and bakes them as fixed text-embedding inputs into the graph,
# producing a YOLOv8-style detector with one class per line below.
# Per-camera config picks a *subset* of these prompts; the Rust detector
# filters detections to the requested subset before emitting them.
#
# When a customer needs a prompt that isn't here, regenerate this file +
# rerun `tools/models/gen_yolo_world.py` — the model artifact is
# reproducible and the manifest sha256 will refresh automatically.
#
# Order matters (the export bakes class indices in this order). Prefer
# *appending* new prompts so existing per-camera configs keep their
# class-id semantics.

person
vehicle
car
truck
bus
motorcycle
bicycle
forklift
trailer
backpack
handbag
suitcase
package
delivery box
shopping cart
dog
cat
bird
deer
firearm
rifle
handgun
knife
hardhat
helmet
high-visibility vest
safety glasses
gloves
ladder
scissor lift
trash bag
spray paint can
crowbar
hammer
shovel

# --- Construction-site equipment (added 2026-05-18 for cam1
#     "Construction Site" YOLO-World override; appended so existing
#     class indices 0-34 are preserved per the file-header rule).
excavator
crane
dump truck
cement mixer
bulldozer
wheelbarrow
traffic cone
jersey barrier
scaffolding

# --- SPEC-035 Pass A: harm-taxonomy actors & objects (per-frame hot
#     path). Acts, objects, and conditions only — never a category of
#     person (ADR-042); `balaclava` is admitted for being behaviourally
#     specific while a generic `mask` is prohibited by HARM_TAXONOMY §9.
#     Appended so existing class indices are preserved. The manifest
#     `prompts` list and sha256 refresh when `gen_yolo_world.py
#     --all-static` is re-run against these lines (deferred model cut).
bolt cutter
angle grinder
pry bar
sledgehammer
screwdriver
jack
gas can
lighter
broken glass
graffiti
syringe
glass pipe
tent
tarp
sleeping bag
mattress
machete
baseball bat
balaclava

# --- SPEC-035 Pass B: scene fixtures for the slow-cadence fixture pass
#     (scene-map anchors, not the per-frame detector). Appended for the
#     same reproducible re-export.
door
roll-up door
gate
fence
window
security camera
light fixture
dumpster
pallet
HVAC unit
ATM
cash register
safe
vending machine
bike rack
charging station
parking stall
