# CODEOWNERS — review gate for the `main` branch ruleset.
#
# The ruleset on `main` requires Code Owner review on every PR
# (`require_code_owner_review: true`). A member of
# @Keystone-Infrastructure-Corp/nexus-maintainers other than the PR
# author must approve before merge.
#
# @andboyer is the sole `bypass_actor` on the ruleset, in `pull_request`
# mode — used only when no other maintainer is available to review.
# All other maintainers go through normal code-owner review.

*       @Keystone-Infrastructure-Corp/nexus-maintainers
